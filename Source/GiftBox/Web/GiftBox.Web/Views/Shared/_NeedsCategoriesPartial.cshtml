@using GiftBox.Common
@using GiftBox.Web.ViewModels
@model GiftBox.Web.Views.Categories.ListNeedCategoryViewModel
@using (Ajax.BeginForm("FilterByNeedCategory", "Need",
                                new AjaxOptions
                                {
                                    HttpMethod = "GET",
                                    InsertionMode = InsertionMode.Replace,
                                    UpdateTargetId = "all-needs",
                                }))
{
    @Html.EditorFor(m => m.CategoryId, new { ListItem = Model.Categories })
}